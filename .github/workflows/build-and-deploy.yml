name: Build & Deploy
on: 
  push:
    branches:
      - master
jobs:
  build:
      runs-on: ubuntu-latest
      env: 
        token: ${{ secrets.LINKS_TOKEN }}
        key: ${{ secrets.SSH_PRIVATE_KEY }}
      steps:
        - uses: actions/checkout@v3 
#         - uses: actions/setup-node@v3
#           with:
#             node-version: 18
#         - name: Install
#           run: npm ci
#         - name: Build
#           run: npm run build
        - name: Install SSH private key
          run: |
            install -m 600 -D /dev/null ~/.ssh/id_rsa
            echo $key > ~/.ssh/id_rsa
        - name: Deploy
          run: | 
            link=`curl -sv -H "Authorization: Bearer $token" -H "Accept: application/vnd.github.v3.raw" https://api.github.com/repos/quicknotes-project/links/contents/link.txt`
            proto=$(echo $link | grep :// | sed -e's,^\(.*://\).*,\1,g')
            url=$(echo ${link/$proto/})
            user=$(echo $url | grep @ | cut -d@ -f1)
            hostport=$(echo ${url/$user@/} | cut -d/ -f1)
            host=$(echo $hostport | sed -e 's,:.*,,g')
            port=$(echo $hostport | sed -e 's,^.*:,:,g' -e 's,.*:\([0-9]*\).*,\1,g' -e 's,[^0-9],,g')
            mkdir build
            echo "test" > build/file
            rsync -zav --delete -e "ssh -o StrictHostKeyChecking=no -p $port" build/ pi@$host:/home/pi/minimal-openresty-server/dist/ 
